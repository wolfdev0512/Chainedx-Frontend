import React from "react";

// components
import Header from "components/Header/Header";

const Home: React.FC = () => {
  return (
    <div>
      <Header />
    </div>
  );
};

export default Home;
